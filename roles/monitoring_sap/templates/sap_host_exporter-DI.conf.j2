# The listening TCP/IP address and port.
address: "0.0.0.0"
port: "5{{ sap_monitoring_nr }}{{ '%02d' % (ansible_loop.index + 4) }}"
log-level: "info"
# sap_monitoring_solution_name: {{ sap_monitoring_solution_name }}
# DI instance
sap-control-url: "http://{{ (sap_app_server[(ansible_loop.index - 1)].ip) }}:{{ (sap_app_server[(ansible_loop.index -1)].port) }}"
